const renderPost = ( post, idx, cats ) => {
    console.log(acf);
    return(
        <div key={ idx } className={`blog-post col-md-4`}>
            test
        </div>
    )
}

export default renderPost;